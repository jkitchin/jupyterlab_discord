{
  "jupyter.lab.shortcuts": [
    {
      "args": {},
      "command": "discord:open",
      "keys": ["Ctrl O", "D"],
      "selector": "body"
    }
  ],
  "title": "jupyterlab_discord",
  "description": "jupyterlab_discord settings.",
  "type": "object",
  "jupyter.lab.menus": {
    "main": [
      {
        "id": "jp-mainmenu-chat",
        "label": "Discord",
        "rank": 80,
        "items": [
          {
            "command": "discord:open"
          }
        ]
      }
    ]
  },

  "properties": {
    "discord_server": {
      "type": "string",
      "title": "Discord server",
      "description": "The number for the Discord server, as a string.",
      "default": "1408876219008553041"
    },

    "discord_channel": {
      "type": "string",
      "title": "Discord channel",
      "description": "The number for the Discord channel, as a string.",
      "default": "1408876219008553044"
    },
    "discord_height": {
      "type": "string",
      "title": "Discord widget height",
      "description": "The height in pixels as a string.",
      "default": "600"
    },

    "discord_width": {
      "type": "string",
      "title": "Discord widget width",
      "description": "The width in pixels as a string.",
      "default": "800"
    }
  },

  "additionalProperties": false
}
